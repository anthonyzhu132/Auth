import React from 'react';
import { View } from 'react-native';

const Spinner = () => {
  return (
    <View>
      <Text>
        Spinner.js rendering
      </Text>
    </View>
  );
};

export default Spinner;
